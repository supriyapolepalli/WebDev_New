@page
@model ManagementEvent.Pages.EventTypes.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>EventType</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.EventType.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.EventType.Name)
        </dd>
    </dl>
</div>s

<div>
    <a asp-page="./Edit" asp-route-id="@Model.EventType.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
